<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.number_field :room_id %>
  </div>
  <div class="field">
    <%= f.label :size %><br>
    <%= f.select(:size, Room::ROOM_SIZES) %>
  </div>
  <div class="field">
    <%= f.label :building %><br>
    <%= f.select(:size, Room::BUILDING) %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.select(:size, Room::ROOM_STATUS) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
